# AI 行为基本准则 (Strict Rules)

## Git 安全操作
- **严禁执行** `git checkout` 命令，无论是否带参数。
- **严禁执行** `git reset --hard`、`git clean` 或任何会导致工作区代码丢失的操作。
- 如果你需要撤销更改，请明确询问用户，严禁私自覆盖未提交的代码。

## 开发偏好
- 这是一个 React 项目，使用 Vite 打包。
- 所有的修改必须基于当前分支，禁止尝试切换分支。
- 每次完成重大逻辑修改后，必须提示用户进行手动 Commit。
